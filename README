== libsha1 ==

libsha1 is a tiny library providing SHA1 implementation, created for
facilitating X server compilation on devices when larger libraries containing
SHA1 implementations (OpenSSL, Mozilla NSS) are not needed.
